/* CSS files add styling rules to your content */
@import url('https://fonts.googleapis.com/css2?family=Nanum+Gothic&display=swap');




html {
	overflow-x: hidden;
	scroll-behavior: smooth;
}

  h1{
    color: Black;
    text-align: center;
    font-size: 50px;
	padding-right: 200px;
	padding-left: 200px;
	/* text-decoration: underline; */
	letter-spacing: .2rem;
	font-family: "Righteous", sans-serif;

}

  p{
	margin-left: 100px;
	margin-right: 200px;
	font-size: 20px;
	font-family: "Righteous", sans-serif;
	font-weight: 500;
	line-height: 1.3;
}

  hr{
   margin-left: 100px;
   margin-right: 100px;
   font-size: 75px;
   font-family: "Righteous", sans-serif;
}
  
  .grid-container { /*Creates grid for my website*/
    display: grid;
    grid-template-rows: 600px 50px 900px 100px;
    height: 100%;
}

  .header {
	box-sizing: border-box;
	background-position: center;
    background-repeat: no-repeat;
    background-size: cover;
	background-image: url('../images/maxresdefault.jpg');
}
	
.image {
	width: 100%;
	position: relative;
	height: 70vh;
}

.center {
  display: block;
  margin-left: auto;
  margin-right: auto;
  width: 50%;
  padding-top: 300px;
}

  .nav{
    background-color: #000000;
}

/* CSS for block menu starts here */
ul {
  display: inline;
  padding: 15px 0px 0px 0px;  /*change here*/
  list-style: none;
}

ul li {
  font-family: Arial;
  font-size: 16px;
  font-weight: bold;
  display: inline-block;
  margin-right: -4px;
  position: relative;
  padding: 15px 20px 17px; /*padding for main block menu buttons */
}

ul li:hover {
  background: #FFD533;
  }
  
/* Hover Colours*/
a:link {color: #FFFFFF;} /* unvisited link */
a:visited {color: white;} /* visited link */
a:hover {color: #1c1a54;} /* mouse over link */
a:active {color: #FFFFFF;}  /* selected link */

a {
text-decoration: none;
}

ul li ul {
  padding: 0;
  position: absolute;
  top: 48px; /* space between each drop-down menu list */
  left: 0;
  width: 175px; /* width of drop-down menu list */
  display: none;
  visibility: hidden; /* this hides the drop-down menu list */
  z-index: 1; /* for overlapping divs, bringing to the front like a drop down menu */
}

ul li ul li { 
  background: #FFD533; /* this is the drop-down menu background color */
  display: block; 
}

ul li ul li:hover { background: #85b7ec;} /* this is the drop-down menu hover on blocks */

ul li:hover ul {
  display: block;
  visibility: visible; /* this makes the drop-down menu visable*/
}

/* Style for Navigation ends here*/

  .content{
    background-color: #FFFFFF;
}
  
/* To set up and call a Class from the HTML File we use a . in the CSS.*/
.slider {
    position: relative;
    width: 44%;  /* adjustment to show each image on carousel. */
    overflow: hidden;  /* overflow hides next image. */
	margin-top: 100px;
}
.images {
    display: flex;
    width: 100%;
}
.images img {
    height: 700px;
    width: 100%;
    transition: all 0.15s ease;  /* timing transition to show next image. */
}
.images input {
    display: none;  /* display none hides the 3 images behind 1st image. */
}
.dots {
    display: flex;
    justify-content: center;
    margin: 5px;
}
.dots label {
    height: 20px;
    width: 20px;
    border-radius: 50%;
    border: solid black 3px;
    cursor: pointer;
    transition: all 0.15s ease;  /* timing transition to show next image. */
    margin: 5px;
}

.dots label:hover {background: black;}
#img1:checked ~ .m1 {  /* ~ (tilde) is a selector used to select each "m" element following checked. */
    margin-left: 0;
}
#img2:checked ~ .m2 {
    margin-left: -100%;
}
#img3:checked ~ .m3 {
    margin-left: -200%;  /* margin-left value moves each image to the Left. */
}
#img4:checked ~ .m4 {
    margin-left: -300%;
}



  .scroll-area{
    background-color: #FFFFFF;
	min-height: 1000px;
}
  
  /* CSS for styling my Back to Top Button */
  
  #myBtn {
	  display: none;
	  position: fixed;
	  bottom: 20px; /* Start for Friday Week 2 */
	  right: 30px;
	  z-index: 99; /* z-index brings the element (button) to the front */
	  font-size: 18px;
	  border: none;
	  outline: none;
	  background-color: black; /* Calling up my Variable Color to be used. */
	  color: yellow;
	  cursor: pointer; /* Setting the icon to be used for my button on hover. */
	  padding: 15px;
	  border-radius: 4px;
}

   #myBtn:hover {
	   background-color: grey; /* Calling up my Variable Color to be used. */
}
   
  .footer{
    background-color: #000000;
	color: white;
}
